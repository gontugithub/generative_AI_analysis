# generative_AI_analysis

source .venv/bin/activate
pip install -r requirements.txt
pip freeze > requirements.txt 

git pull // bajas los cambios 

git status
------------------------------

git add .  // añades todos los cambios

git commit -m "he añadido un funcion"

git push

holaaa
ffff
